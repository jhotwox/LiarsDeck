services:
  nginx:
    image: nginx:alpine
    container_name: lyingcards-proxy
    restart: unless-stopped

    ports:
      - "3002:3001"

    extra_hosts:
      - "host.docker.internal:host-gateway"

    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro